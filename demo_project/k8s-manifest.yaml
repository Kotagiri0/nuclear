# Kubernetes manifest с секретами (неправильно!)

apiVersion: v1
kind: Pod
metadata:
  name: my-app
spec:
  containers:
  - name: app
    image: myapp:latest
    env:
    # 🔴 Пароль в явном виде
    - name: DB_PASSWORD
      value: "SuperSecretPassword123!"
    
    # 🔴 API ключ в явном виде  
    - name: API_KEY
      value: "sk_live_abcdefghijklmnopqrstuvwx"
    
    # 🔴 AWS credentials
    - name: AWS_ACCESS_KEY_ID
      value: "AKIAIOSFODNN7EXAMPLE"
    - name: AWS_SECRET_ACCESS_KEY
      value: "wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY"
    
    # ✅ Правильный подход - использовать Secret
    # - name: DB_PASSWORD
    #   valueFrom:
    #     secretKeyRef:
    #       name: app-secrets
    #       key: db-password

---
# Dockerfile с проблемами
# FROM python:3.11
# ENV API_KEY=sk_live_abcdefghijklmnopqrstuvwx
# ENV DATABASE_URL=postgres://admin:password@db:5432/app
