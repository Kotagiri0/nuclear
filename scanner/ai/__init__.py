"""Optional AI-powered checks (e.g. code security scanning).

This package is intentionally optional: it should not be imported unless the
user explicitly enables AI scanning via CLI/API, because it may require extra
dependencies and external network calls.
"""

